using System.Windows.Controls;

namespace WpfApp5;

public partial class unlockAvatar : Page
{
    public unlockAvatar()
    {
        InitializeComponent();
            //Sb.Completed += new EventHandler(); // add the event handler to the completed event of the animation
            //Sb.Begin(); // start the animation
    }

}